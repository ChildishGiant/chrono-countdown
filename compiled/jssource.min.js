function sOrNah(e, t) {
    document.getElementById("time").innerHTML += 1 == e ? "1 " + t + " " : e + " " + t + "s ";
}

for (var now = new Date(), ref = new Date(15011964e5), testDay = ref; testDay < now; ) testDay = testDay.setDate(testDay.getDate() + 14);

testDay.setHours(16), window.onload = function() {
    function e() {
        var e = new Date(), s = testDay - e;
        if (s < 0) return clearInterval(r), void (document.getElementById("time").innerHTML = "EXPIRED!");
        var i = Math.floor(s / o), D = Math.floor(s % o / a), d = Math.floor(s % a / n), f = Math.floor(s % n / t);
        document.getElementById("time").innerHTML = "", sOrNah(i, "day"), sOrNah(D, "hour"), 
        sOrNah(d, "min"), sOrNah(f, "second");
    }
    var t = 1e3, n = 60 * t, a = 60 * n, o = 24 * a, r = window.setInterval(function() {
        e();
    }, 1e3);
    e();
};